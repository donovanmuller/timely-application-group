server.port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/donovanmuller/timely-application-group-configuration
          searchPaths: '{application}'
